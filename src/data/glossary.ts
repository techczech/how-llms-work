export interface GlossaryEntry {
  id: string
  term: string
  definition: string
  category: string
  /** Step IDs where this term is most relevant */
  relatedSteps: string[]
  /** Optional: related glossary term IDs */
  seeAlso?: string[]
}

export const glossaryCategories = [
  { id: 'text-to-numbers', label: 'Text to Numbers', description: 'How text gets converted into numbers the model can process' },
  { id: 'model-structure', label: 'Model Structure', description: 'The building blocks of the neural network' },
  { id: 'attention-mechanism', label: 'Attention Mechanism', description: 'How tokens look at and learn from each other' },
  { id: 'learning', label: 'Learning & Training', description: 'How the model improves by adjusting its parameters' },
  { id: 'generation', label: 'Generation & Output', description: 'How the trained model produces new text' },
  { id: 'big-picture', label: 'The Big Picture', description: 'Architecture concepts and scaling' },
] as const

export const glossary: GlossaryEntry[] = [
  // ── Text to Numbers ──
  {
    id: 'token',
    term: 'Token',
    category: 'text-to-numbers',
    definition: 'The smallest unit of text that a language model works with. In microgpt, each character is a token. In production models like ChatGPT, tokens are usually chunks of characters (like common words or word pieces). The model never sees raw text — it sees a sequence of token IDs (numbers).',
    relatedSteps: ['tokenizer', 'task'],
    seeAlso: ['vocabulary', 'bpe', 'bos'],
  },
  {
    id: 'vocabulary',
    term: 'Vocabulary',
    category: 'text-to-numbers',
    definition: 'The complete set of tokens a model knows about. microgpt has 27 tokens (26 lowercase letters + BOS). GPT-4 has roughly 100,000 tokens. A model can only output tokens from its vocabulary — it literally cannot produce a character or word piece it hasn\'t seen during tokenizer construction.',
    relatedSteps: ['tokenizer'],
    seeAlso: ['token', 'bpe'],
  },
  {
    id: 'bos',
    term: 'BOS (Beginning of Sequence)',
    category: 'text-to-numbers',
    definition: 'A special token that marks where a document starts and ends. When the model generates a BOS token, it means "I\'m done." In ChatGPT, there are similar special tokens that mark where your message ends and the assistant\'s reply begins.',
    relatedSteps: ['tokenizer', 'inference'],
    seeAlso: ['token'],
  },
  {
    id: 'bpe',
    term: 'BPE (Byte Pair Encoding)',
    category: 'text-to-numbers',
    definition: 'The tokenization method used by most production language models. It starts with individual characters, then repeatedly merges the most common adjacent pairs into single tokens. The word "the" might be one token, while a rare word like "zephyr" gets split into pieces. This makes the model more efficient because it can process more text per step.',
    relatedSteps: ['tokenizer', 'scaling'],
    seeAlso: ['token', 'vocabulary'],
  },
  {
    id: 'embedding',
    term: 'Embedding',
    category: 'text-to-numbers',
    definition: 'A list of numbers (a vector) that represents a token or position. The model can\'t work with raw token IDs — it needs rich numerical representations. The embedding table is a lookup: token ID 4 ("e") maps to a specific list of 16 numbers. These numbers are learned during training, so similar tokens end up with similar embeddings.',
    relatedSteps: ['embeddings', 'parameters'],
    seeAlso: ['token', 'vector'],
  },

  // ── Model Structure ──
  {
    id: 'parameter',
    term: 'Parameter',
    category: 'model-structure',
    definition: 'A single number inside the model that gets adjusted during training. Parameters start as small random numbers, and training gradually shapes them to encode useful patterns. microgpt has 4,192 parameters. GPT-4 has over a trillion. The parameters collectively ARE the model\'s "knowledge."',
    relatedSteps: ['parameters', 'training', 'optimizer'],
    seeAlso: ['weight', 'gradient'],
  },
  {
    id: 'weight',
    term: 'Weight / Weight Matrix',
    category: 'model-structure',
    definition: 'A grid (matrix) of parameters. Neural networks organize their parameters into matrices like the embedding table or attention projections. "Weights" and "parameters" are often used interchangeably. A "weight matrix" is just a rectangular array of learnable numbers.',
    relatedSteps: ['parameters', 'attention', 'mlp'],
    seeAlso: ['parameter', 'state-dict'],
  },
  {
    id: 'state-dict',
    term: 'State Dict',
    category: 'model-structure',
    definition: 'A dictionary (lookup table) that organizes all the model\'s weight matrices by name. Borrowed from PyTorch terminology. For example, state_dict["wte"] is the token embedding table, state_dict["layer0.attn_wq"] is the query weight matrix for layer 0. It\'s like a filing cabinet for the model\'s knowledge.',
    relatedSteps: ['parameters'],
    seeAlso: ['parameter', 'weight'],
  },
  {
    id: 'vector',
    term: 'Vector',
    category: 'model-structure',
    definition: 'A list of numbers. In this context, a vector represents a token\'s state at a given point in the network. microgpt uses 16-number vectors; GPT-4 uses ~12,000-number vectors. The more numbers, the more nuance the model can capture. All the model\'s internal computations involve manipulating these vectors.',
    relatedSteps: ['embeddings', 'attention'],
    seeAlso: ['embedding', 'dimension'],
  },
  {
    id: 'dimension',
    term: 'Dimension (n_embd)',
    category: 'model-structure',
    definition: 'The size of the model\'s internal vectors. microgpt uses dimension 16, meaning each token is represented by 16 numbers. GPT-4 uses ~12,288. Larger dimensions let the model represent more subtle patterns, but require more computation and memory. This is one of the main knobs for scaling a model up.',
    relatedSteps: ['parameters', 'embeddings'],
    seeAlso: ['vector', 'parameter'],
  },
  {
    id: 'mlp',
    term: 'MLP (Multilayer Perceptron)',
    category: 'model-structure',
    definition: 'A two-layer mini-network inside each transformer layer. It expands the vector to 4× its size, applies a nonlinearity (ReLU), then compresses back. Unlike attention (which lets tokens communicate), the MLP processes each token independently. Think of attention as "gathering information" and MLP as "thinking about it."',
    relatedSteps: ['mlp'],
    seeAlso: ['relu', 'attention', 'residual-connection'],
  },
  {
    id: 'relu',
    term: 'ReLU (Rectified Linear Unit)',
    category: 'model-structure',
    definition: 'A simple function: if the input is positive, keep it; if negative, output zero. Written as max(0, x). This "nonlinearity" is what gives neural networks their power. Without it, stacking multiple layers would be mathematically equivalent to a single layer, and the model couldn\'t learn complex patterns.',
    relatedSteps: ['autograd', 'mlp'],
    seeAlso: ['mlp'],
  },
  {
    id: 'residual-connection',
    term: 'Residual Connection (Skip Connection)',
    category: 'model-structure',
    definition: 'Adding a layer\'s input back to its output: output = layer(x) + x. Like having an express lane alongside the regular road. This lets information and gradients flow directly through the network, making deep models trainable. Without residual connections, networks deeper than ~10 layers fail to learn.',
    relatedSteps: ['attention', 'mlp'],
    seeAlso: ['gradient', 'layer'],
  },
  {
    id: 'rmsnorm',
    term: 'RMSNorm (Root Mean Square Normalization)',
    category: 'model-structure',
    definition: 'A technique that rescales a vector so its values stay within a reasonable range. Without normalization, values can grow or shrink as they pass through layers, causing training to become unstable. It\'s like periodically recalibrating an instrument to keep measurements accurate.',
    relatedSteps: ['embeddings'],
    seeAlso: ['layer'],
  },

  // ── Attention Mechanism ──
  {
    id: 'attention',
    term: 'Attention',
    category: 'attention-mechanism',
    definition: 'The mechanism that lets tokens look at and gather information from earlier tokens in the sequence. Each token asks "what am I looking for?" (query), earlier tokens advertise "what do I have?" (key), and if there\'s a match, information flows (value). This is the ONLY place where tokens communicate with each other.',
    relatedSteps: ['attention'],
    seeAlso: ['query-key-value', 'attention-head', 'kv-cache'],
  },
  {
    id: 'query-key-value',
    term: 'Query, Key, Value (Q, K, V)',
    category: 'attention-mechanism',
    definition: 'Three vectors computed for each token in the attention mechanism. Think of it like a search engine: the Query is your search query, the Keys are page titles, and Values are the page content. The model computes how well each Query matches each Key (dot product), then retrieves a weighted mix of the Values. Tokens whose keys best match the query contribute most.',
    relatedSteps: ['attention'],
    seeAlso: ['attention', 'attention-head'],
  },
  {
    id: 'attention-head',
    term: 'Attention Head',
    category: 'attention-mechanism',
    definition: 'One independent attention computation. Each head can learn to look for different patterns — one might track the previous character, another might look for vowel patterns. Multiple heads run in parallel, and their outputs are combined. microgpt has 4 heads; GPT-4 has 96+.',
    relatedSteps: ['attention'],
    seeAlso: ['attention', 'query-key-value'],
  },
  {
    id: 'kv-cache',
    term: 'KV Cache',
    category: 'attention-mechanism',
    definition: 'A memory that stores the Key and Value vectors from all previous tokens so they don\'t need to be recomputed. When generating token #50, the model needs to attend to tokens #1–49. Instead of reprocessing all of them, it just looks up their cached Keys and Values. This is crucial for fast inference in production systems.',
    relatedSteps: ['attention', 'training', 'inference'],
    seeAlso: ['attention', 'query-key-value'],
  },
  {
    id: 'softmax',
    term: 'Softmax',
    category: 'attention-mechanism',
    definition: 'A function that converts a list of arbitrary numbers (logits) into a probability distribution — all positive and summing to 1.0. Larger inputs get exponentially more probability mass. Used both in attention (to weight how much each past token matters) and at the output (to get the probability of each possible next token).',
    relatedSteps: ['attention', 'mlp', 'training', 'inference'],
    seeAlso: ['logit', 'temperature'],
  },

  // ── Learning & Training ──
  {
    id: 'gradient',
    term: 'Gradient',
    category: 'learning',
    definition: 'A number that tells you: "if I increase this parameter by a tiny amount, how much does the loss change?" Positive gradient means increasing the parameter increases the loss (bad), so we should decrease it. Negative gradient means increasing it decreases the loss (good). Gradients are the compass that guides training.',
    relatedSteps: ['autograd', 'training', 'optimizer'],
    seeAlso: ['backpropagation', 'loss', 'chain-rule'],
  },
  {
    id: 'backpropagation',
    term: 'Backpropagation (Backward Pass)',
    category: 'learning',
    definition: 'The algorithm that computes gradients for every parameter in one efficient sweep. It starts at the loss and works backward through the computation graph, using the chain rule to calculate how each parameter contributed to the final loss. One forward pass computes the answer; one backward pass tells us how to improve it.',
    relatedSteps: ['autograd', 'training'],
    seeAlso: ['gradient', 'chain-rule', 'autograd'],
  },
  {
    id: 'chain-rule',
    term: 'Chain Rule',
    category: 'learning',
    definition: 'A rule from calculus that lets you compute the derivative of a composite function by multiplying the derivatives along the chain. If a car is 2× faster than a bike, and the bike is 4× faster than walking, the car is 2×4=8× faster than walking. Backpropagation is just the chain rule applied systematically through the computation graph.',
    relatedSteps: ['autograd'],
    seeAlso: ['gradient', 'backpropagation'],
  },
  {
    id: 'autograd',
    term: 'Autograd (Automatic Differentiation)',
    category: 'learning',
    definition: 'A system that automatically tracks every mathematical operation and can compute gradients through the entire chain. In microgpt, the Value class wraps numbers and records how they were computed. When you call .backward(), autograd traces back through every operation to compute all gradients. PyTorch\'s autograd does the same thing, just on tensors instead of scalars.',
    relatedSteps: ['autograd'],
    seeAlso: ['gradient', 'backpropagation', 'value-class'],
  },
  {
    id: 'value-class',
    term: 'Value (Autograd Wrapper)',
    category: 'learning',
    definition: 'The building block of microgpt\'s autograd engine. Each Value wraps a single number and remembers: (1) the number itself (.data), (2) its gradient (.grad), and (3) how it was computed (children + local gradients). Every math operation creates a new Value that records the computation, building up a graph that backward() can trace.',
    relatedSteps: ['autograd'],
    seeAlso: ['autograd', 'gradient'],
  },
  {
    id: 'loss',
    term: 'Loss (Cross-Entropy Loss)',
    category: 'learning',
    definition: 'A single number that measures how wrong the model\'s predictions are. Specifically, it\'s the negative log of the probability the model assigned to the correct next token: -log(p). If the model is confident and correct (p ≈ 1), loss ≈ 0. If it\'s surprised by the right answer (p ≈ 0), loss → ∞. The entire goal of training is to make this number smaller.',
    relatedSteps: ['training'],
    seeAlso: ['gradient', 'softmax'],
  },
  {
    id: 'forward-pass',
    term: 'Forward Pass',
    category: 'learning',
    definition: 'Running input data through the model to get a prediction. Tokens go in → embeddings → attention → MLP → logits come out. This is the "thinking" step. During training, we compare the prediction to the correct answer to compute the loss. During inference, we just use the prediction directly.',
    relatedSteps: ['training', 'inference'],
    seeAlso: ['backpropagation', 'logit'],
  },
  {
    id: 'training-loop',
    term: 'Training Loop',
    category: 'learning',
    definition: 'The repeating cycle that teaches the model: (1) pick training data, (2) run the forward pass, (3) compute loss, (4) run the backward pass to get gradients, (5) update parameters using the optimizer. Repeat thousands to millions of times. Each cycle makes the model slightly better at predicting the next token.',
    relatedSteps: ['training'],
    seeAlso: ['forward-pass', 'backpropagation', 'optimizer-adam'],
  },
  {
    id: 'optimizer-adam',
    term: 'Adam Optimizer',
    category: 'learning',
    definition: 'A smart algorithm for updating parameters during training. Instead of just subtracting the gradient (basic gradient descent), Adam keeps a running average of past gradients (momentum — like a rolling ball) and adapts the learning rate for each parameter individually. Parameters with noisy gradients get smaller updates. Used in virtually all modern LLM training.',
    relatedSteps: ['optimizer'],
    seeAlso: ['gradient', 'learning-rate', 'parameter'],
  },
  {
    id: 'learning-rate',
    term: 'Learning Rate',
    category: 'learning',
    definition: 'How big a step to take when updating parameters. Too large: the model overshoots and training is unstable. Too small: training is painfully slow. microgpt uses 0.01, decaying to 0 over training — big steps early to explore, small steps late to refine. Getting this right is critical; the wrong learning rate can waste the entire training run.',
    relatedSteps: ['optimizer', 'training'],
    seeAlso: ['optimizer-adam', 'parameter'],
  },
  {
    id: 'epoch',
    term: 'Epoch / Training Step',
    category: 'learning',
    definition: 'One training step processes one document (or batch of documents). microgpt runs 1,000 steps, each processing one name. An epoch is one complete pass through the entire dataset. The loss decreases from ~3.3 (random guessing) to ~2.37 as the model learns the statistical patterns.',
    relatedSteps: ['training'],
    seeAlso: ['training-loop', 'loss'],
  },

  // ── Generation & Output ──
  {
    id: 'logit',
    term: 'Logit',
    category: 'generation',
    definition: 'A raw, unnormalized score that the model outputs — one per token in the vocabulary. Before softmax, these are just numbers (positive or negative) where higher means "more likely." After softmax converts them to probabilities, we can sample from them or pick the most likely token.',
    relatedSteps: ['mlp', 'inference'],
    seeAlso: ['softmax', 'temperature'],
  },
  {
    id: 'inference',
    term: 'Inference (Generation)',
    category: 'generation',
    definition: 'Using a trained model to generate new text. The model starts with a prompt (or just BOS), predicts the next token, samples one, feeds it back in, and repeats. The parameters are frozen — no learning happens. This is what ChatGPT does when you chat with it: autoregressive token-by-token generation.',
    relatedSteps: ['inference'],
    seeAlso: ['temperature', 'sampling', 'forward-pass'],
  },
  {
    id: 'temperature',
    term: 'Temperature',
    category: 'generation',
    definition: 'A "creativity dial" that controls how random the model\'s output is. Logits are divided by temperature before softmax. Temperature = 1.0 uses the model\'s natural distribution. Lower values (like 0.5) make the model more conservative and predictable. Higher values make it more random and creative. Temperature → 0 always picks the single most likely token.',
    relatedSteps: ['inference'],
    seeAlso: ['softmax', 'logit', 'sampling'],
  },
  {
    id: 'sampling',
    term: 'Sampling',
    category: 'generation',
    definition: 'Randomly choosing the next token according to the probability distribution (as opposed to always picking the most likely one). This controlled randomness is what makes generated text varied and natural-sounding. Each time you run the model, you might get different output because of sampling.',
    relatedSteps: ['inference'],
    seeAlso: ['temperature', 'softmax'],
  },
  {
    id: 'hallucination',
    term: 'Hallucination',
    category: 'generation',
    definition: 'When a model generates something that sounds plausible but isn\'t real. microgpt "hallucinates" names like "karia" that don\'t exist in the training data. ChatGPT hallucinating a fake citation is the same phenomenon at a larger scale — the model produces statistically plausible completions that happen not to be factually correct.',
    relatedSteps: ['inference', 'scaling'],
    seeAlso: ['inference', 'sampling'],
  },

  // ── The Big Picture ──
  {
    id: 'layer',
    term: 'Layer (Transformer Layer)',
    category: 'big-picture',
    definition: 'One complete processing block consisting of attention (communication) followed by MLP (computation), each with residual connections. Data flows through one layer, then the next. microgpt has 1 layer; GPT-4 has 120+. More layers = more rounds of processing = more sophisticated reasoning.',
    relatedSteps: ['parameters', 'attention', 'mlp', 'scaling'],
    seeAlso: ['attention', 'mlp', 'residual-connection'],
  },
  {
    id: 'context-window',
    term: 'Context Window (Block Size)',
    category: 'big-picture',
    definition: 'The maximum number of tokens the model can "see" at once. microgpt\'s context window is 16 tokens (the longest name is 15 characters). GPT-4 can handle 128K+ tokens. The model can only attend to tokens within this window — anything before it is forgotten. This is why very long conversations sometimes lose earlier context.',
    relatedSteps: ['parameters', 'attention'],
    seeAlso: ['attention', 'token'],
  },
  {
    id: 'transformer',
    term: 'Transformer',
    category: 'big-picture',
    definition: 'The neural network architecture used by all modern LLMs. Its core idea: alternate between attention (letting tokens communicate) and MLP (processing information locally), connected by residual connections and normalization. Introduced in the 2017 paper "Attention is All You Need." GPT, BERT, LLaMA, and Claude are all transformers.',
    relatedSteps: ['task', 'attention', 'mlp', 'scaling'],
    seeAlso: ['attention', 'mlp', 'layer'],
  },
  {
    id: 'gpt',
    term: 'GPT (Generative Pre-trained Transformer)',
    category: 'big-picture',
    definition: 'A type of transformer that generates text by predicting one token at a time, left to right. "Generative" = it produces text. "Pre-trained" = trained on a huge text dataset before being fine-tuned for specific tasks. "Transformer" = the architecture. microgpt is a minimal GPT; ChatGPT is a very large one with additional post-training.',
    relatedSteps: ['task', 'scaling'],
    seeAlso: ['transformer', 'post-training'],
  },
  {
    id: 'post-training',
    term: 'Post-Training (SFT + RLHF)',
    category: 'big-picture',
    definition: 'Additional training that turns a text-completing model into a helpful chatbot. SFT (Supervised Fine-Tuning) trains the model on curated conversations. RLHF (Reinforcement Learning from Human Feedback) teaches it what humans consider helpful, harmless, and honest. microgpt skips this — it\'s a pure pre-trained model.',
    relatedSteps: ['scaling'],
    seeAlso: ['gpt', 'training-loop'],
  },
  {
    id: 'tensor',
    term: 'Tensor',
    category: 'big-picture',
    definition: 'A multi-dimensional array of numbers. A single number is a 0D tensor. A list is a 1D tensor (vector). A table is a 2D tensor (matrix). microgpt uses plain Python lists and individual numbers. Production models use tensors on GPUs for massive parallel computation — same math, much faster.',
    relatedSteps: ['scaling', 'autograd'],
    seeAlso: ['vector', 'parameter'],
  },
]
